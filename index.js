exports.handler = async (event) => {
    // TODO implement for testings 21
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello from Git Oauth!'),
    };
    return response;
};
