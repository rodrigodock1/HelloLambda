exports.handler = async (event) => {
    // TODO implement for testings 3
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello from Git Oauth!'),
    };
    return response;
};
