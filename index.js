exports.handler = async (event) => {
    // TODO implement for testings 32
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello from Git Oauth!'),
    };
    return response;
};
