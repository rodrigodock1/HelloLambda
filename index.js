exports.handler = async (event) => {
    // TODO implement for testings 23
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello from Git Oauth!'),
    };
    return response;
};
